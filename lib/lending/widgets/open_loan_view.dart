import 'package:flutter/material.dart';
import 'package:librarian_app/widgets/placeholder_view.dart';

class OpenLoanView extends StatelessWidget {
  const OpenLoanView({super.key});

  @override
  Widget build(BuildContext context) {
    return const PlaceholderView(title: "Open a new Loan");
  }
}
